# Cписок основных команд и понятий
-----------------------------------
pwd показать рабочую папку
/c/ или c:/ - путь к диску
cd / перейди в корневую директорию
cd ~ Перейти к домашней директории
ls Вывести содержимое директории
cd имя_папки сменить директорий
cd .. переход на уровень выше
cd . переходим в текущую директорию
cd github/open-source-project переходим через несколько директорий
ls -a  вывели список, в котором отображаются скрытые файлы ., .. и .git
ls ~ выведет содержимое домашней директории вне зависимости от того, что показывает pwd
ls .. покажет содержимое родительской директории
touch my-new-file.txt создали файл my-new-file.txt в текущей папке
touch index.html style.css script.js — если нужно создать сразу несколько файлов, можно напечатать их имена в одну строку через пробел

mkdir new-dir создали директорию new-dir в текущей папке
mkdir -p dir1/dir-inside/dir-deeper-inside создали папку dir-deeper-inside в папке dir-inside, которая находится в папке dir1 
*По умолчанию touch и mkdir создают файлы и папки в текущей рабочей директории

mkdir ~/my-git-projects создаст папку my-git-projects внутри домашней директории

touch ../../file.txt создаст файл file.txt на две папки выше по иерархии. 
т.е projects/git/hello, команда touch ../../file.txt создаст файл по такому пути: projects/file.txt

cp что_копируем куда_копируем
cp file.txt ~/my-dir (от англ. copy, «копировать») — скопируй файл в другое место

cp что_копируем что_копируем что_копируем куда_копируем
т.е cp index.html style.css script.js src/ скопировали три файла (index.html, style.css и script.js) в папку src 

mv Перемещение файлов и папок
mv file.txt ~/my-dir (от англ. move, «переместить») — перемести файл или папку в другое место

cat Чтение файлов

rm, rmdir, rm Удаление файлов и папок
rm -r images удалили папку images со всем её содержимым из текущей директории 

cd c:/ + Enter или cd /c + Enter в корневую папку попадаем

git version 
git config --global user.name "User Namovich" имя или ник нужно написать латиницей и в кавычках

git config --global user.email username@yandex.ru здесь нужно указать свой настоящий email 
cat ~/.gitconfig 
git config --list 

Команды необязательно печатать и выполнять по очереди. Можно указать их списком — разделить двумя амперсандами (&&)

Чтобы не вводить название файла или папки полностью, можно набрать первые символы имени и дважды нажать Tab. 
Если файл или папка есть в текущей директории, командная строка допишет путь сама

cd ~/dev/first-project # перешли в нужную папку

! git init # создали репозиторий 

rm -rf .git Разгитить» папку, если что-то пошло не так
cd <папка с репозиторием> # перешли в папку
rm -rf .git # удалили подпапку .git 

! git status Проверить состояние репозитория

! git add Подготовить файлы к сохранению

! git add --all # подготовили к сохранению все файлы в репозитории

//можно по отдельности
git add todo.txt
git add readme.txt

git add . добавить в репозиторий текущую папку со всеми файлами

git commit Выполнить коммит

! git commit -m 'Мой первый коммит!' с присвоением сообщения

[master (root-commit) baa3b6e] значит: коммит был в ветке master;
root-commit — это самый первый, или «корневой» (англ. root), коммит в ветке, у следующих коммитов такой надписи не будет;
baa3b6e — сокращённый идентификатор коммита (подробнее об этом мы ещё расскажем).
2 files changed, 1 insertion(+) значит: изменились два файла (readme.txt и todo.txt);
одна строка была добавлена (1. Пройти пару уроков по Git.).
Строки вида create mode 100644 readme.txt — это более подробная информация о новых (добавленных в Git) файлах.
create (англ. «создать») говорит, что файл был создан. Если бы файл был удалён, на этом месте было бы слово delete (англ. «удалить»).
mode 100644 сообщает, что это обычный файл. 
Также возможны варианты 100755 для исполняемых файлов (например, что-нибудь.exe) и 120000 для файлов-ссылок в Linux. 
Файлы-ссылки не содержат данных сами по себе, а только ссылаются на другие файлы — как «ярлыки» в Windows.
 
! git log Просмотреть историю коммитов

ls -la .ssh/ вывели список созданных ключей 
ls -a ~/.ssh
ls -la ~/.ssh

ssh-keygen -t ed25519 -C "электронная почта, к которой привязан ваш аккаунт на GitHub" создание ssh ключа

ssh-keygen -t rsa -b 4096 -C "электронная почта, к которой привязан ваш аккаунт на GitHub" (другое шифрование)
  
git remote add origin git@github.com:%ИМЯ_АККАУНТА%/first-project.git привязать локальный и удаленный репозитории

git remote -v Убедиться, что репозитории связаны (Флаг -v — короткая форма флага --verbose (англ. «подробный»). 
Он позволяет показать больше информации в выводе.)

! git push -u origin main Отправить изменения на удалённый репозиторий	# Если команда приведёт к ошибке, попробуйте Первый push с флагом -u
                          						# заменить main на master. 
II
V
Команда git clone сама свяжет удалённые и локальные ветки в момент клонирования (при коммите изменений в выгруженный Clone проект)

git clone Клонировать репозиторий (укажите адрес репозитория, который нужно склонировать во вкладке CODE SSH)

! Если ввести git commit без флага -m, откроется редактор Vim. 
Чтобы выйти из него, нажмите клавишу Esc, наберите последовательность символов :q! и нажмите Enter

Команда git clone скопирует проект на локальный компьютер и автоматически свяжет локальный репозиторий с удалённым. 
Форк» выполняется через графический интерфейс GitHub и позволяет получить копию репозитория, которая полностью независима от оригинального проекта

**[My profile Git-Hub:] (https://github.com/Nadezhdushka92)**